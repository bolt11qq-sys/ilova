# Keep line endings sane across Windows and the Linux CI runner.
* text=auto eol=lf

# Windows-only scripts keep CRLF so they run from Explorer too.
*.ps1 text eol=crlf
*.bat text eol=crlf

# Never touch binaries.
*.png binary
*.jpg binary
*.jpeg binary
*.ttf binary
*.jar binary
