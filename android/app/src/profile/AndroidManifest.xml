<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Profile builds only, for the same reason as debug. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
