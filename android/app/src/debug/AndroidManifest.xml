<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug builds only: the Flutter tool talks to the device over a socket. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
